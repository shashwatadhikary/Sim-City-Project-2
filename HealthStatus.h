// HealthStatus.h
#ifndef HEALTH_STATUS_H
#define HEALTH_STATUS_H

// Define the HealthStatus enum to represent different health statuses
enum HealthStatus {
    HEALTHY,
    SICK,
    RECOVERING,
    DEAD
};

#endif
